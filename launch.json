{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "lldb", // Use "cppvsdbg" if you're on MSVC
        "request": "launch",
        "name": "Run Rust Program",
        "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}.exe",
        "args": [],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "cargo build",
        "terminal": "integrated"
      }
    ]
  }
  